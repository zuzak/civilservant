/* eslint-env mocha */

describe('urban module', function () {
  it('should look up an urban dictionary definition')
  it('should handle a bad response')
  it('should handle a non-existent entry')
})
