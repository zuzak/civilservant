/* eslint-env mocha */

describe('usage module', function () {
  it('should tell you the usage of a command with one argument')
  it('should tell you the usage of a command with multiple arguments')
  it('should handle a command with no arguments')
  it('should handle a command with unspecified arguments')
})
