/* eslint-env mocha */

describe('codes module', function () {
  it('should look up a known airport')
  it('handle looking up an unknown airport')

  it('should look up a known currency')
  it('handle looking up an unknown currency')

  it('should look up a known language')
  it('handle looking up an unknown language')

  it('should look up a known country')
  it('handle looking up an unknown country')

  it('should look up a known state')
  it('handle looking up an unknown state')
})
