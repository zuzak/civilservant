/* eslint-env mocha */

describe('reload module', function () {
  it('should reload a module')
  it('should list the running modules')
  it('should pull from remote')
  it('should list the current Git hash')
  it('should respond to CTCP VERSION')
})
